<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
  <div class="alert alert-dissable alert-<%= flash_class(key) %>">
    <button class="close" data-dismaiss="alert">x</button>
    <%= value %>
  </div>
  <% end %>
<% end %>
